# Port for the local Express server (default: 3001)
SERVER_PORT=3001

# Absolute or relative path where generated images are saved
# Relative paths are resolved from the project root
IMAGES_DIR=./saved-images
